apiVersion: apps/v1
kind: Deployment
metadata:
  name: jaltest
  namespace: jaltest
  labels:
    app.kubernetes.io/name: jaltest
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
       maxSurge: 1
       maxUnavailable: 1
  selector:
    matchLabels:
      app: jaltest
  template:
    metadata:
      labels:
        app: jaltest
    spec:
      containers:
        - name: app
          image: ghcr.io/cse-labs/imdb-app:latest
          imagePullPolicy: Always
          args: 
          - --in-memory
          - --zone
          - central-tx-dfw
          - --region
          - central
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            limits:
              cpu: 500m
              memory: 512Mi

---
apiVersion: v1
kind: Service
metadata:
  name: jaltest
  namespace: jaltest
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: jaltest
    
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: jaltest-https
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`central-tx-dfw-101.cseretail.com`)
      kind: Rule
      services:
        - name: jaltest
          namespace: jaltest
          kind: Service
          port: 8080
  tls:
    secretName: ssl-cert

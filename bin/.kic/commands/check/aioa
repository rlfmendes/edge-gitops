#!/bin/bash

#name: ai-order-accuracy
#short: Check Order Accuracy status on the local cluster

# change to this directory
#cd "$(dirname "${BASH_SOURCE[0]}")" || exit

http "https://${AKDC_FQDN}/version"

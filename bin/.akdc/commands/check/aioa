#!/bin/bash

#name: ai-order-accuracy
#short: Check AI Order Accuracy status on the local cluster

# change to this directory
#cd "$(dirname "${BASH_SOURCE[0]}")" || exit

http localhost:30080/version

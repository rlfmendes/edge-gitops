#!/bin/bash

# name: run
# short: Run GitOps in the current directory (GitOps must be configured)

# change to this directory
#cd "$(dirname "${BASH_SOURCE[0]}")" || exit

docker run -it --rm -v "$PWD":/ago ghcr.io/retaildevcrews/autogitops:beta -r "$AKDC_REPO" -p "$AKDC_PAT"
